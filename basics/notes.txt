JIT and AOT Compilation
JİT LARGER FİLE SİZE
AOT SMALL FİLE SİZE
JİT LOADS SLOWER SİNCE COMPİLER NEEDS TO RUN İN BROWSER
aot loads faster since the app is compiled on delivery to the BROWSER
jit catch errors in browser 
aot catch errors in server
jit compile code when needed
aot compile code on the server

There are 2 types of directives
Attribute Directives and Structural Directives
Both types of directive have different syntax rules and behaviors
AD changes the appearance or behavior of an element
for ex. if we want to dynamically add styles to an element, this type of directive would be considered an attribute directive
SD can add or remove elements in the document
